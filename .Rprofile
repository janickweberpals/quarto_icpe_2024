source("renv/activate.R")

# For reproducibility, freeze package versions

# Mac OS
if(Sys.info()['sysname'][[1]]=="Darwin") options(repos = c(REPO_NAME = "https://packagemanager.posit.co/cran/2024-06-01"))
